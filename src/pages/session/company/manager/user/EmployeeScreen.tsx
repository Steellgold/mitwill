import type { ReactElement } from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

export const EmployeeScreen = (): ReactElement => {
  return (
    <View>
      <Text>EmployeeScreen</Text>
    </View>
  );
};